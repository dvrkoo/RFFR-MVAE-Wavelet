# PM-VAE 3-branch wavelet residual classifier, 1 fake frame per video.
metadata:
  experiment_name: "f2f_pm_vae_3branch_fake1"
  comment: "F2F PM-VAE 3-branch, fake1"

dataset:
  protocol: "F2F_All_Fake1"
  max_fake_frames: 1

logging:
  wandb:
    tags: ["f2f", "pm_vae", "3branch", "wavelet", "fake1"]
